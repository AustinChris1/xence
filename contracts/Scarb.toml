[package]
name = "xence"
version = "0.1.0"
edition = "2024_07"
description = "Xence: sealed forecast bonds and public calibration, settled through the STRK20 privacy pool."
license = "Apache-2.0"

[dependencies]
starknet = "2.20.0"

[dev-dependencies]
cairo_test = "2.20.0"

[[target.starknet-contract]]
sierra = true
casm = true
